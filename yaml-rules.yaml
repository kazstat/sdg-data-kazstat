extends: default

rules:
    line-length: disable
    new-lines: disable
    document-start: disable
    trailing-spaces: disable
    new-line-at-end-of-file: disable
    comments: disable
    colons: disable

